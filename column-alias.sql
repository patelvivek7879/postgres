-- SELECT first_name, last_name as surname FROM customer limit 50

-- SELECT first_name || ' ' || last_name AS full_name FROM customer;;


SELECT first_name || ' ' || last_name AS "Full Name" FROM customer LIMIT 20;
